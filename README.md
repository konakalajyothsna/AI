# What is AI?

This project compares traditional rule-based programming with a model-based text classifier.

The rule-based program follows conditions written by a human.
The model-based program uses learned language patterns to make a prediction for new text.
